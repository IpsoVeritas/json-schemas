{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$id": "https://developer.brickchain.com/schemas/receipt.json",
    "title": "Receipt",
    "description": "This is the Receipt schema for Brickchain documents, used for issuing receipts from services, see more at https://developer.brickchain.com/",
    "type": "object",
    "allOf": [ { "$ref": "https://developer.brickchain.com/schemas/base.json" } ],
    "properties": {
        "role": {
            "type": "string",
            "description": "The Role that executed the Action that formed the Receipt"
        },
        "action": {
            "type": "string",
            "description": "The Action document that formed the Receipt"
        },
        "uri": {
            "type": "string",
            "description": "The URI for doing updates on the Receipt"
        },
        "jwt": {
            "type": "string",
            "description": "A JWT to be used when doing updates on the Receipt"
        },
        "intervals": {
            "type": "array",
            "items": {
                "type": "object",
                "description": "An Interval, start and end time for i.e. an event or booking"
            },
            "description": "An array of Intervals, used for events or bookings"
        },
        "label": {
            "type": "string",
            "description": "The label describing the Receipt"
        },
        "params": {
            "type": "array",
            "items": {
                "type": "string",
                "description": "TODO: perhaps remove this, an param used for the Action"
            },
            "description": "TODO: perhaps remove this, all extra params used for the Action"
        }
    }
}
