{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$id": "https://developer.brickchain.com/schemas/action.json",
    "title": "Realm Descriptor",
    "description": "This is the Realm Descriptor schema for Brickchain documents, see more at https://developer.brickchain.com/",
    "type": "object",
    "allOf": [ { "$ref": "https://developer.brickchain.com/schemas/base.json" } ],
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the Realm"
        },
        "description": {
            "type": "string",
            "description": "The description of the Realm"
        },
        "publicKey": {
            "type": "object",
            "description": "The public JWK of the Realm"
        },
        "endpoints": {
            "type": "array",
            "items": {
                "type": "string",
                "format": "uri"
            },
            "description": "Versioned API endpoints for the Realm"
        },
        "inviteURL": {
            "type": "string",
            "format": "uri",
            "description": "The Invite URL for the Realm"
        },
        "servicesURL": {
            "type": "string",
            "format": "uri",
            "description": "Location of where to find services published by the Realm"
        },
        "keyHistory": {
            "type": "array",
            "items": {
                "type": "string",
                "description": "A serialized JWK of a public key"
            },
            "description": "A list of historical keys used by the Realm"
        },
        "icon": {
            "type:": "string",
            "format": "uri",
            "description": "The location of the icon used for displaying the Realm"
        },
        "banner": {
            "type": "string",
            "format": "uri",
            "description": "The location of the banner used for displaying the Realm"
        }
    }
}
