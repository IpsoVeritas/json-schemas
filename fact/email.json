{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$id": "https://developer.brickchain.com/schemas/fact/email.json",
    "title": "Base",
    "description": "This is the schema for the e-mail fact type, having one attribute that contains an e-mail address.",
    "type": "object",
    "properties": {
        "email": {
            "type": "string",
            "format": "email",
            "description": "An e-mail address."
        }
    },
    "required": [
        "email"
    ]
}