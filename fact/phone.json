{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "$id": "https://developer.brickchain.com/schemas/fact/phone.json",
    "title": "Base",
    "description": "This is the schema for the phone fact type, having one attribute that contains a phone number.",
    "type": "object",
    "properties": {
        "phone": {
            "type": "string",
            "description": "A phone number."
        }
    },
    "required": [
        "phone"
    ]
}